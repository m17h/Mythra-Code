{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Mythra Code desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-confirm",
    "notification:default",
    "opener:default",
    "updater:default",
    "process:allow-restart"
  ]
}
